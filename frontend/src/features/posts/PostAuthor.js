import React from 'react'



export const PostAuthor = ({ userId }) => {

  // const author = useSelector(state => selectUserById(state, userId))

  // return <span> {author ? author.name : 'Неизвестный автор'}</span>
  <span></span>
}
